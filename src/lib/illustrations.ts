/**
 * Named Storyset illustrations — see src/lib/storyset.ts for paths & fetch script.
 * Prefer <Illustration name="…" /> or storysetSrc() for new UI.
 */
export {
  STORYSET_ILLUSTRATIONS as ILLUSTRATIONS,
  type StorysetIllustrationName as IllustrationName,
} from '@/lib/storyset';
